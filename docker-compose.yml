services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: todo-harbor-app
    restart: unless-stopped
    ports:
      - "18080:3000"
    volumes:
      - ./data:/app/data
